{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "InviteeConversion",
	"description": "Schema for the Conversion Stream owned by the Invitee. A new Stream is created for every new Conversion. Each Conversion Stream is Indexed by the Network.",
	"type": "object",
	"properties": {
		"id": {
			"description": "The Blockchain Address of the Usher Campaign.",
			"type": "string"
		},
		"sig": {
			"description": "The Network signature verifying that this conversion originated via an affiliate",
			"type": "string"
		},
		"invitee_id": {
			"description": "An identifer native to the Network for the invitee (referred user). Could be a wallet address, or client associated identifer.",
			"type": "string"
		},
		"event_id": {
			"description": "The event indentifier indicating which event in the campaign was converted",
			"type": "integer",
			"minimum": 0
		},
		"native_id": {
			"description": "An identifier native to the Advertiser applied to the invitee referred user. Used to control the number of conversions processed for a given user.",
			"type": "string"
		},
		"metadata": {
			"description": "Additional data associated to this conversion",
			"type": "array",
			"items": {
				"type": "object",
				"properties": {
					"key": {
						"type": "string"
					},
					"value": {
						"type": ["boolean", "string", "number"]
					}
				},
				"required": ["key", "value"]
			}
		},
		"commit": {
			"description": "An arbitrary number indicating how much of the campaign event limit this conversion consumed",
			"type": "integer"
		}
	},
	"required": ["sig", "event_id"]
}
